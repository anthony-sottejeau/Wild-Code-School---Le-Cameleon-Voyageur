 <section class="team container-fluid">
    <div class="col-md-offset-2 col-md-8">
        <div class="col-xs-12">
            <h2>L'équipe</h2>
        </div>
        <div class="row">
            {% for team in Team %}
                {% if loop.index is odd %}
                    <div class="col-xs-12">
                        <div class="row center-team">
                                <div class="col-xs-3 img-container">
                                    <img class="img-circle" src="{{ team.picture }}" alt="{{ team.name }}">
                                </div>
                                <div class="col-xs-9 description-container">
                                <h3>{{ team.name }}</h3>
                                <p>{{ team.description }}</p>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="col-xs-12">
                        <div class="row center-team">
                            <div class="col-xs-9 description-container">
                                <h3>{{ team.name }}</h3>
                                <p>{{ team.description }}</p>
                            </div>
                            <div class="col-xs-3 img-container">
                                <img class="img-circle" src="{{ team.picture }}" alt="{{ team.name }}">
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</section>