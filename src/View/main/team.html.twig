<section class="team container-fluid">
    <div class="col-md-offset-2 col-md-8">
        <div class="col-xs-12">
            <h2>L'équipe</h2>
        </div>
        <div class="row">
            {% for teamMate in team %}
                <div class="col-xs-12">
                    <div class="row center-team">
                        <div class="col-xs-3 img-container {{ loop.index is odd ? 'col-xs-push-9' }}">
                            <img class="img-circle" src="{{ teamMate.picture }}" alt="{{ teamMate.name }}">
                        </div>
                        <div class="col-xs-9 description-container {{ loop.index is odd ? 'col-xs-pull-3' }}">
                            <h3>{{ teamMate.name }}</h3>
                            <p>{{ teamMate.description }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</section>