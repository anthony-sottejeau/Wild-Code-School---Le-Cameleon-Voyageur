{% extends 'main/body.html.twig' %}
{% block content %}
    <section id="team" class="container-fluid">
        <div class="col-md-offset-2 col-md-8">
            <div class="col-xs-12">
                <h2><span class="glyphicon glyphicon-bookmark bookmark-prev"></span> L'équipe <span class="glyphicon glyphicon-bookmark bookmark-next"></span></h2>
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <div class="row">
                        <div class="col-xs-4 img-container">
                            <div>
                                <img class="img-circle" src="{{ TeamMate[0].picture }}" alt="{{ TeamMate[0].alt }}">
                            </div>
                        </div>
                        <div class="col-xs-8 description-container">
                            <h3>{{ TeamMate[0].name }}</h3>
                            <p>{{ TeamMate[0].description }}</p>
                        </div>
                    </div>
                </div>
                <div class="col-xs-12">
                    <div class="col-xs-8 description-container">
                        <h3>{{ TeamMate[1].name }}</h3>
                        <p>{{ TeamMate[1].description }}</p>
                    </div>
                    <div class="col-xs-4 img-container">
                        <img class="img-circle" src="{{ TeamMate[1].picture }}" alt="{{ TeamMate[1].alt }}">
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}